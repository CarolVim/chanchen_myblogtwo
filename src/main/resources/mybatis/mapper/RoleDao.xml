<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.myblog.dao.RoleDao">
    <select id="findSingle" parameterType="User" resultType="User">
        select userId,userName,level from user
    </select>
</mapper>